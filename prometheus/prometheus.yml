global:
  scrape_interval: 30s
  scrape_timeout: 10s
  evaluation_interval: 30s

scrape_configs:
  - job_name: github_release_notifier
    static_configs:
      - targets:
          - app:8080
    metrics_path: /metrics
