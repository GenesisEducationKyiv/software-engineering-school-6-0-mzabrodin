# Subscription service (cmd/subscription) — every variable config.Load() reads.

# Public API
PORT=8080
BASE_URL=http://localhost:8080

# Message broker — publishes subscription lifecycle events (the outbox relay)
NATS_URL=nats://localhost:4222

# Datastores
DATABASE_URL=postgres://your_username:your_password@localhost:5432/your_database?sslmode=disable
REDIS_URL=redis://:your_redis_password@localhost:6379

# Confirmation tokens (stateless JWTs)
JWT_SECRET=your_jwt_secret
CONFIRM_TOKEN_TTL=24h

# Pending-subscription cleanup ticker
PENDING_CLEANUP_INTERVAL=24h

# GitHub
GITHUB_TOKEN=your_github_token

# API key
API_KEY=your_api_key

# Log level (debug, info, warn, error)
LOG_LEVEL=info
