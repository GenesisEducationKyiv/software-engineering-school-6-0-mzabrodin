# Subscription service (cmd/subscription) — every variable config.Load() reads.

# Public API
PORT=8080
BASE_URL=http://localhost:8080

# Internal scanner link it dials (mTLS)
SCANNER_ADDR=localhost:50051

# Message broker — publishes email commands for the emailer to consume
NATS_URL=nats://localhost:4222

# mTLS material — mounts the client cert (it only dials the scanner)
TLS_CERT_FILE=certs/subscription.crt
TLS_KEY_FILE=certs/subscription.key
TLS_CA_FILE=certs/ca.crt

# Datastores
DATABASE_URL=postgres://your_username:your_password@localhost:5432/your_database?sslmode=disable
REDIS_URL=redis://:your_redis_password@localhost:6379

# Scan schedule (drives the scanner)
SCAN_INTERVAL=10m

# GitHub
GITHUB_TOKEN=your_github_token

# API key
API_KEY=your_api_key

# Log level (debug, info, warn, error)
LOG_LEVEL=info
